[package]
name = "rpc-tester"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
pathdiff = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
